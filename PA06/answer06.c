#include <stdio.h>
#include "answer06.h"

void printstep()
void find_directions(char ** maze, int w, int h, int ini_x, int ini_y)
{
  int ind = 0;
  int dex = 1;
  while (ind <= w)
  {
     if (maze[dex][ind]= ' ')
     { dex++;
       while (maze[dex][ind]=' ')
       {
         
       } 
       print("%c %n",direction, )
     }  
    ind++;
  }


}
void print_directions(char ** maze, int w, int h)
